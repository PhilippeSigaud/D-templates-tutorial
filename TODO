Sorting Types: the Variant example

Polymorphic Tree: the Document example 

auto doc = 
document(
    title("Ranges: A Tutorial"),
    author("John Doe"),
    tableOfContents,
    
    section(
        title("Some Range Definition"),
        "Ranges are a nice D abstraction...",
        definition(
            "Input Range", 
            "The most basic kind of range, it must define the following methods:",
            list(definition("front", "..."),
                definition("popFront", "..."),
                definition("empty", "..."))
        )
    )
    section(
        title("Some ranges examples"),
        "...",
        code(
            "auto m = map!((a) => a*a)([0,1,2,3]);
             assert(m.length == 4);"
        ),
        link("http://dlang.org/", "Website")
    )
    section(
        title("Beyond ranges"),
        "..."
    )
);

auto latex = document.to!"LaTeX";
auto html = docuent.to!"HTML";
            